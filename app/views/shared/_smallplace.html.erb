		<table>
			<tr>
				<td >
					<% place = Place.find(place) %>
					<a class="pull-left" href="<%= place_path(place.id) %>">
		                <img class="media-object dp img-circle" src="<%= place.pcover.url(:thumb) %>" style="width: 50px;height:50px;">
		            </a>
		        </td>
				<td>
					<h4>&nbsp;&nbsp; <%= link_to place.name, place %></h4>
				</td>
			</tr>
			
		</table>   
		            
		                
		                
		