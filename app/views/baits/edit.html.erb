<div class="page-header">
  <%= link_to baits_path, class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to @bait, class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Show
  <% end %>
  <%= link_to 'Delete', @bait, :method => 'delete', :confirm => 'Are you sure?', class: 'btn btn-primary'%>
  <h1>Editing bait</h1>
</div>

<%= render 'form' %>
